These scripts are used entirely for overlap checking.

They simplify the overlap checks down to simple convex shapes.
The shapes/boundaries are all 2d but can be extruded.

This package only works currently for static boundaries.
The place to modify to make dynamic boundaries would be the cache, but it's probably worth reevaluating the approach.
